> 最后更新：2026-08-24 01:47

# Codex 专属落盘与日常开发规则

## 蓝图施工 Auto-Approve（零询问）

- 蓝图代码施工、Landing 文件落位、**已指明**的文件删除、配套 Python / PowerShell：**一律直接执行，不再询问**。
- 细则：`.cursor/rules/19-blueprint-construction-auto-approve.mdc`；Cursor 个人规则同名条目已同步。
- 例外仍有效：未点名 SO/Prefab、未要求的 git 提交推送、全库迁移、架构二选一。

## 笔记工作区

- Unity 项目知识文档统一写入：`F:\Code\Note\3.Core-Drive_Action\3D ARPG`。
- Unity 工程只保存运行所需的源码、资产、`.meta`、ProjectSettings、测试与构建配置；不得重新在 `Assets` 下建立 AI 文档库。
- 目录职责以 Note 根目录的 `TREE.md` 为准。
- 需求大类目录名为 **`01_需求与迭代`**（不再使用 `01_需求与范围`）。
- 提醒类内容只在对话中回复，不为提醒单独创建文件。

## CodeMap 优先扫描与施工路由（20-codemap-first-scan）

- 代码施工、代码分析、调用链梳理和架构评审，优先审查：`F:\Code\Note\3.Core-Drive_Action\3D ARPG\12_版本与发布\06_CodeMap代码地图`。
- 固定顺序：`0.CodeMap方法论.md` / `1.初级CodeMap制作计划书.md`（首次或规则变化时）→ `10.SystemOverview.md` → `11.DependencyGraph.md` → 相关 `20_Modules\{Module}.md` → Map 指向的源码入口、消费者和测试。
- 默认只在 `F:\Code\UnityProject\Core-Drive Action\Assets\GameMain\Scripts` 内按类型/方法/字段用 `rg` 定点读取；不得先对工程盘、Note 盘或整个 Unity 工程做递归扫描。
- CodeMap 未命中或与现码冲突时，按“相关模块目录 → 相邻消费者/测试 → Scripts 根内符号搜索”有界扩展；仍无法定位才报告更宽扫描的必要性，并记录原因和未扫描范围。
- CodeMap 是静态路由，不替代源码事实和编译/测试/Play 验证；冲突以现码为准，并记录地图过期与回写范围。入口、依赖方向、数据所有权、禁止项、风险或测试映射变化时，同一 Landing 回写对应 CodeMap。
- 施工前必须记录：`CodeMap files consulted`、`Affected Modules`、权威单点、共享类型、`Must not touch`、`Migration needed?`（Affected ≥3 时先写 Migration Plan）、源码定点路径和待回写 Map 文件。
- 完整规则见 `.cursor/rules/20-codemap-first-scan.mdc`。

## 显式【需求】落位（规则 17）

- 用户显式标明 `【需求】` / 新需求时：**自动**落需求说明 + 设计书（可按双属性多落位到多个子文件夹、不同 `【类型】` 标题）。
- **禁止**因此自动创建施工蓝图；须人工确认设计书后再写 `05_代码落位蓝图`。
- 细则：`.cursor/rules/17-requirement-explicit-auto-landing.mdc`。

## 一事一号（规则 18）

- 同一件事的 `【需求】` / `【探索问题】` / `【决策】` / `【蓝图】` / 设计书 / `【Output】` 等 **共用统一任务标号**；用 `【类型】` 区分，不为类型换号。
- 必要时可用二级 / 三级标号做同任务切片（例 `227.3`、`227.3.1`～`227.3.6`）。
- 迭代与修 Bug：**非用户点名新主题时不起新号**；迭代设计/细节进 `04_设计说明书与迭代开发/07_迭代开发/`；代码 Output 优先追加原 Output，或写入 `06_开发记录/.../80_迭代与修复/`；机制/阶段总结进 `09_决策记录与总结/05_机制与阶段总结/`。
- 细则：`.cursor/rules/18-unified-topic-numbering.mdc`。

## 225 起的编号与目录

- `001–224` 是旧工作流，只读归档于 `06_开发记录\00_旧开发日志_001-224`，不得改号、覆盖或继续追加新主题。
- 新主题从 `225` 开始递增。**一个任务标号**对应一件可独立说明和验收的事，不等于一天或一次 AI 对话。
- 需求写入 `01_需求与迭代\`；设计写入 `04_设计说明书与迭代开发\`；同号迭代设计/记录写入该目录下 `07_迭代开发\`。
- 蓝图写入 `05_代码落位蓝图\对应十号段`（仅设计确认后）。
- Output 与日常施工记录写入 `06_开发记录\01_开发记录_225起\对应十号段`；代码向迭代/修复写入该段下 `80_迭代与修复\`。
- 模块长期演化同步写入 `07_模块开发记录\对应模块`。
- AI 原始输出、评审与待人工验证写入 `08_AI输出管理`。
- 问题、失败和根因写入 `10_问题与修复方案`；机制/阶段 `【总结】` 写入 `09_决策记录与总结/05_机制与阶段总结/`；验收指南、Log 和验收结论写入 `11_测试与验收`。

## 同系列链接与依赖

- 同一任务标号下的总蓝图、各 Landing、Output、验收指南和验收结论都必须在 YAML 的 `series_files` 中互相列出，并在正文提供“同系列文件导航”。
- `series_files` 只表示同一开发主题的查阅关系，不代表执行顺序。
- `depends_on` 只记录当前文件真正需要先成立的文档或 Landing；禁止为了互链把同系列所有文件都写入 `depends_on`。
- 稳定设计依据写入 `source_baselines`，不得与 `depends_on` 混用。

## 文档命名与时间戳

- 文件名优先：`{任务标号}【类型】主题.md`；同任务切片再用 `{任务标号}.{切片}【类型】…`（可三级）。
- 常用类型：`【需求】`、`【探索问题】`、`【系统设计】`、`【蓝图】`、`【开发任务】`、`【Output】`、`【决策】`、`【总结】`、`【AI评审】`、`【问题】`、`【修复】`、`【迭代】`、`【验收指南】`、`【Log】`、`【验收结论】`、`【版本总结】`。
- `225+` 的结构化日常文档采用 Obsidian YAML Properties：第一行必须是 `---`，并在属性中填写 `created` 与 `updated`；YAML 结束后第一行正文再写可见的 `> 产出时间：YYYY-MM-DD HH:mm` 或 `> 最后更新：YYYY-MM-DD HH:mm`。
- 不使用 YAML 的普通文档仍在第一行写可见时间戳。旧 `001–224` 文档不为适配新格式而批量改写；规则 18 生效前已落的 `228.1` / `228.10` 等不强制改名。

## 设计消费与蓝图施工状态前缀

- 状态标签紧跟任务标号、位于文档类型之前：`227.5.1【To be used】【系统设计】…`、`227.5.1【Used】【系统设计】…`、`227.5.2【TODO】【蓝图】…`、`227.5.2【Done】【蓝图】…`。
- 设计书状态：
  - `【To be used】`：已形成设计，但当前确认范围尚未完整消费为可施工蓝图。
  - `【Used】`：当前确认范围已被一个或多个正式蓝图引用，并已转写为代码落点、Landing、非目标和验收门禁；不表示对应代码已施工。
  - 若一份设计同时包含近期与远期范围，应先拆出相应蓝图或明确延后蓝图；未被蓝图承接的设计范围不得误标 `【Used】`。
- 蓝图状态：
  - `【TODO】`：对应代码/资产尚未落位；蓝图刚创建时默认使用该状态。
  - `【Done】`：蓝图负责的代码/资产已经落位并完成风险相称的静态/编译验证；它只回答“本蓝图是否已被消费施工成现码”，**不等待** Unity Test Runner、Play、资产装配、Profiler 或实机验收。未运行的验收必须在正文与 Output 中保持 OPEN。
  - 验收发现行为 Bug、回退或契约偏离时，不把已施工蓝图退回 `【TODO】`；按同一任务号进入“探针/原始证据 → 根因诊断 → 修复方案或迭代设计 → 迭代/修复蓝图 → 代码 Output/验收”的独立闭环。只有蓝图本身仍有未施工代码/资产范围时才保持或改回 `【TODO】`。
- Codex 自动管理状态：生成蓝图后检查并更新被消费设计的前缀；完成 Landing 后检查并更新对应蓝图前缀。禁止只改正文状态而不改文件名。
- 每次前缀变化必须同步：YAML `status/updated/revision`、`series_files`、`depends_on/source_baselines` 中的 WikiLink、正文“同系列文件导航”、蓝图索引、Project Hub 和模块记录中的入口链接。
- 旧 `001–224` 归档不批量套用；规则生效前的历史文件只在当前任务实际消费或施工时渐进迁移。

## 每次 Landing 的最少落位

1. 开工前确认对应蓝图、当前 Landing、非目标和验收门禁。
2. 修改 Unity 代码或资产，并完成风险相称的静态、编译、测试或实机验证。
3. 立即更新对应 `【Output】`，记录修改文件、生命周期、运行时数据流、蓝图差异、验证事实、OPEN 和下一 Landing。
4. 更新 `00_Project-Hub\01_当前进度与下一步.md`。
5. 更新对应模块的 `03_开发进程.md`；没有代码变化时明确标记为文档、规则或验收辅助工作，不伪装成代码 Landing。

同一 Output 内的多个 Landing 按事实顺序使用独立分割线 `---`，并用独立一行的 `==【1】==`、`==【2】==` 递增标识，不倒排历史。

## 模块开发记录

- 已在 `001–224` 期间形成的模块统一标记“模块记录待补充”，补写历史本身不消耗新的主序号。
- 只有补充过程中实际修改当前代码、资产、设计或验收边界时，才领取新的 `225+` 编号。
- 模块记录必须区分：当前代码事实、历史设计、验证事实、推测和 OPEN。

## AI 代码师傅模式

- 专门规则位于 `.cursor/rules/15-ai-agent-code-mentor.mdc`，适用于代码落位、重构、调试和验收探针修改。
- 生产目标仍是可复用的 3A 动作模板；优先通过 Runtime 能力契约、数据资产、ScriptableObject、Editor 工具和配置入口表达 Player、Enemy、NPC 差异。
- 每次代码 Landing 的 Output 包含讲解字数计划、需求与数据/API 设计、代码编写过程、生命周期、运行时数据流、验证事实、学习内容、手写复现训练、下一能力目标以及 3A 模板与配置化检查。
- 默认讲解预算：普通小 Landing 约 800～1200 字；架构或跨模块迁移约 1200～1800 字；验收或纯配置约 500～800 字。

## Log 与验收事实

- `11_测试与验收\06_Log与运行时探针` 是新 Log 的唯一笔记入口；用户手动植入的原始 Log 只读，Codex 不得擅自改写、覆盖、清理或重排。
- 新 Log 文件必须标注适配蓝图、场景、实体 `instanceId`，并按需要保留 `eventId`、`objectId`、`ActionLease version` 等关联字段。
- 代码探针负责运行时事实，手动 Log 负责时机标注；两者通过稳定标识与时间信息对照。
- `【Log】` 保存原始事实，`【验收指南】` 保存操作方法，`【验收结论】` 保存分析结论；三者不得混写。
- 结论必须区分“Log 已证明”“代码静态确认”“Unity 实测已确认”“尚未验证”和“推测”。

### 行为 Bug / 表现回退的强制闭环顺序

- 对 Locomotion、Animation、Camera、Action、Motor 等运行时行为 Bug 或表现回退，默认且强制采用：**低噪声 Log 探针 → 时机 Log 提取/原始证据 → 根因诊断 → 设计书/修复方案 → 正式蓝图 → 代码施工 → Output/验收**。
- 禁止在没有探针与可关联时机证据时直接凭观感改参数、CrossFade、角速度、Root Motion、KCC 或状态分流；紧急止血也必须明确标为临时措施，并保留补齐证据与正式蓝图的 OPEN。
- 探针必须先写“要证伪什么”，再定义事件字典、关联键、采样窗口、限流与体积预算；默认只采状态边沿、决策边沿、资产选择、播放请求、首帧/中点/结束和异常，不逐帧刷屏。
- 时机 Log 提取必须保存原始事实并按稳定键关联 Input/Gameplay/Presentation/Motor；不得把分析结论改写进 `【Log】`。若当前环境不能取得新的 Play 样本，必须显式区分用户实机观察、既有 Log 已证明、代码静态确认与 Play 待验证，禁止伪造 Runtime 事实。
- 设计书必须先冻结权威边界、状态机时序、数据合同、非目标、降级/回滚和验收矩阵；修复方案必须指出第一个偏离合同的量及最小修复面。蓝图只有在上述依据成立后才能生成。
- 代码施工必须逐条对应蓝图 Landing，新增防回归测试和必要探针，并更新 Output、Project Hub 与模块记录。编译通过不能替代 Unity Test Runner、PlayMode 或实机验收。
- 补偿性动画只能消费 Gameplay 已裁决事实，不得反写 LogicFacing、PlanarVelocity、KCC、Camera Heading 或 TargetLock；表现缺失不得通过恢复 Gameplay 水平缓转来修复。

### Runtime / Debug Log 无堆栈强制规则

- 从本规则生效起，所有新增或修改的 Runtime、Debug、Diagnostics、验收探针 Log **一律不得附带调用堆栈**；这是默认且强制的输出合同，不依赖 Unity Console 的 Stack Trace Logging 设置。
- 探针禁止直接使用会受全局堆栈设置影响的 `Debug.Log`、`Debug.LogWarning`、`Debug.LogError`。统一使用 `Debug.LogFormat(LogType, LogOption.NoStacktrace, context, format, args)`，Warning / Error 级诊断同样显式传入 `LogOption.NoStacktrace`。
- 若某项调查确实需要调用路径，必须由用户明确要求；调用栈应走独立、默认关闭的专项取证开关，不能混入常规 Log，也不能改变“常规 Log 无堆栈”的默认行为。
- Log 必须采用 AI 友好的单行结构化格式，优先稳定短键与关联字段；禁止把多行说明、重复上下文或可由会话头推导的信息逐条复制到每个事件。
- 高频事实必须采用边沿、状态变化、异常阈值、节流心跳或有限采样窗口；禁止无上限逐帧刷屏。新探针应在设计时给出事件字典、采样频率/窗口和预估体积预算。
- Landing 验证必须检查：Unity Console 复制结果不含 `UnityEngine.Debug:*` 或脚本调用栈；持续运行不会无限增长；原始文本可不经清洗直接交给 AI 分析。未完成该检查时，Log 可标记编译通过，但运行时采样质量仍为 OPEN。

## 完成标准

- 代码或资产实际落位，且职责、依赖方向、数据流和生命周期已说明。
- 对应 Output、Project-Hub 和模块开发记录已同步。
- 已执行的验证有证据，未执行的 Unity 编译、装配、PlayMode 或实机步骤明确标为 OPEN。
- AI 输出只有经过评审并转化为代码、正式文档或验证证据后，才可以改变项目完成状态。
