# Codex 专属落盘规则

## 项目开发日志归档

- 除提醒类内容外，凡需要落盘的分析、蓝图、施工记录、评审、版本总结、验收记录及其它知识文档，默认写入：`F:\Code\Note\3.Core-Drive_Action\3D ARPG\2.项目开发日志`。
- 归档目录按当前对话主题及用户贴入或点名的文件编号决定；例如本轮 220.x 内容归入 `211-220`。不得无依据固定使用某个编号范围。
- 若目标编号目录或主题子目录不存在，先创建必要目录，再写入文件。
- 文件名应携带对应编号和主题，便于按项目日志检索；更新已有文档时优先修改原文件，不重复新建同义文件。
- 文档首行遵循项目时间戳规则：新建使用 `> 产出时间：YYYY-MM-DD HH:mm`，实质更新使用 `> 最后更新：YYYY-MM-DD HH:mm`。
- 提醒类内容只在对话中回复，不为提醒单独创建文件。
- 项目运行所必需的源码、Unity 资产、`.meta` 和构建配置仍必须落在工程目录内；本规则约束的是文档与知识产物的归档位置。

## 220.4 日常小落地工作流

- 按 220.4 蓝图每次只推进一个小 Landing，并在代码调整完成后立即做一次小结。
- 每次小结统一追加到 `F:\Code\Note\3.Core-Drive_Action\3D ARPG\2.项目开发日志\211-220\220.4.2 【Output】EntityRuntime_落地总结.md`；若文件不存在则创建，若用户指定了其它同编号 Output 文件则优先使用用户指定文件。
- 小结至少记录：本次 Landing、修改文件、运行时数据流、与蓝图的差异、验证结果、未完成门禁和下一 Landing；首行遵循时间戳规则。
- 同一 Output 文件累计多次落位时，每次小结之间必须使用独立分割线 `---`，并用独立一行的固定格式 `==【标号】==` 标记本次落位；标号按落位顺序递增，不得改用圆括号、半角方括号或仅使用 Markdown 标题代替。
- 当前 220.4.2 Output 已使用 `==【3】==` 标记 A3；后续 A4、A5 依次使用 `==【4】==`、`==【5】==`。
- 代码落地与总结写入是同一工作单元：代码未完成或未验证时，不得把该 Landing 标记为完成。

## 220.x 小落地总结理解导览

- 每次代码小落地的小结，除记录 Landing、修改文件、运行时数据流、蓝图差异、验证结果、未完成门禁和下一 Landing 外，必须新增“本次实现了哪些功能、代码布局、各布局的用途、整体目的”四项说明。
- “功能”说明本次新增或改变了什么行为；“代码布局”说明类型、目录和模块之间如何组织；“用途”说明各模块在运行时承担什么职责；“目的”说明本次小落地如何降低后续理解、验收和继续落位的负担。
- 该导览内容必须使用面向维护者的语言，避免只罗列类名；当本次没有代码调整时，应明确标注为文档规则或验收辅助更新，不伪装成代码 Landing。
- 220.5 及后续 220.x Output 同样遵守本规则，并继续使用独立分割线 `---` 与 `==【标号】==` 累计记录。

## Output 与 Log 分层排序规则

- `Output` 中的代码/资产 Landing 小节必须按事实发生顺序排列：`==【1】==`、`==【2】==`、`==【3】==` 依次递增；不得为了显示最新内容而倒置历史 Landing。
- Log 分析、实机现象分析和验收结论不改变 Landing 的事实顺序；在 Output 或对应验收文档中使用独立的“最新分析（置顶）”区域，按最新分析在上、历史分析在下排列。
- 最新分析置顶区域必须注明来源 Log、适配蓝图序号、分析时间、当前结论和仍缺失的证据；不得把推测写成实机事实。
- 若最新分析对应某次 Landing，正文仍保留该 Landing 的原始 `==【标号】==` 小节；置顶区域只做摘要和索引，避免破坏代码施工过程的时间线。
- 仅调整 Output 排序或增加置顶分析属于文档工作，不占用蓝图 Landing 次数；必须保留既有小节内容和标号，不得删除历史事实。

## 220.x 蓝图落位次数控制

- 后续进入新的 220.x 蓝图后，不再为了教学或过程记录无限细分 Landing；优先按蓝图已经写明的 Landing 合并实施。
- 若蓝图明确列出 N 次 Landing，实际落位最多使用 N+2 次；例如蓝图写明 C1～C6，最多 8 次，通常应争取控制在 6 次内。
- 一次 Landing 可以同时完成同一设施的代码、资产接线、探针和静态验收，不得把单个字段、单个类或单条 Log 再拆成独立 Landing。
- 每次落位仍需在对应 Output 追加小结，但小结服务于项目理解和验收，不反过来制造额外施工阶段。
- 若某次只更新规则、文档或验收，不得伪装成代码 Landing，也不占用蓝图 Landing 次数。

## AI 代码师傅模式

- 专门规则位于 `.cursor/rules/15-ai-agent-code-mentor.mdc`，适用于所有代码落位、重构、调试和验收探针修改。
- 代码师傅模式不得牺牲项目规范性：项目第一目标仍是打造可复用的 3A 动作模板，优先通过 Runtime 能力契约、数据资产、ScriptableObject、Editor 工具和配置入口表达 Player/Enemy/NPC 的差异，禁止为教学制造临时架构或角色专用 Runtime 分支。
- 每次代码落位必须同时带出需求分析、数据/API 设计、代码编写过程、生命周期、运行时数据流、验证事实和手写复现训练；禁止只交付大段生成代码。
- 代码量较大时按小块讲解和落位，优先让使用者理解接口、字段、关键算法和失败路径；教学提问用于建立思考，不得无限阻塞项目推进。
- 每次 Landing 的 Output 小结除项目维护信息外，必须包含“讲解字数计划、代码编写过程、本次学习内容、手写复现训练、下一步能力目标、3A 模板与配置化检查”，并继续遵守 `---` 与 `==【标号】==` 格式。
- 默认讲解预算：普通小 Landing 约 800～1200 字；架构/接口/跨模块迁移约 1200～1800 字；验收或纯配置约 500～800 字。代码正文不计入字数，字数用于控制理解负担，不得为了凑字重复描述。

## AI 时机日志

- `F:\Code\UnityProject\Core-Drive Action\Assets\GameMain\Scripts\6.AI\Log` 是 AI 时机日志专用目录。
- 用户后续会在该目录手动植入时机 Log；Codex 不得擅自改写、覆盖或清理这些手动日志。
- 当前主题属于 220 的 220.4，相关手动日志归入 `6.AI\Log\220`；已有 `220.4.4【Log】验收.md` 优先沿用。
- 220.4 代码验收探针与手动时机日志分工：代码探针负责运行时事实，手动日志负责时机标注；两者通过 `eventId`、`objectId` 及时间信息对照分析。

## Log 蓝图序号适配规则

- 所有新建或更新的验收 Log、运行时探针说明和 Log 模板，必须标注当前正在适配的最新蓝图序号；本轮 220.5 统一使用 `220.5`。
- 文件名至少包含主序号，例如 `220.5【Log】验收.md`；若属于具体验收方案或子阶段，可使用更细序号，例如 `220.5.3【Log】验收.md`，但文档正文首段仍必须明确 `适配蓝图：220.5`。
- Log 模板的元数据区必须记录 `适配蓝图`、场景、实体 `instanceId`，并保留与该蓝图相关的 `eventId`、`objectId`、`ActionLease version` 等关联字段。
- 运行时探针新增或调整时，日志说明必须写明适配序号；不得把 220.4、220.5 或后续 220.x 的日志协议混用而不注明版本。
- “最新适配序号”按当前实际落位的蓝图决定，不按日期猜测；进入 220.6 后，新增 Log 默认标注 `220.6`，历史 220.5 Log 不改名、不覆盖。
- 用户手动植入的原始时机 Log 仍由用户维护；Codex 只负责新建经用户明确要求的模板或说明，不擅自改写、清理或重排已有原始 Log。
