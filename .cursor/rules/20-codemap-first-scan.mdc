---
description: CodeMap 优先扫描与定点施工路由，避免无必要的全库/全盘源码扫描
alwaysApply: true
---

# CodeMap First Scan Protocol

本项目代码施工、代码分析、调用链梳理和架构评审，优先使用：

`F:\Code\Note\3.Core-Drive_Action\3D ARPG\12_版本与发布\06_CodeMap代码地图`

## 固定扫描顺序

1. 先用 `rg --files` 清点 CodeMap 目录；首次接手或地图规则发生变化时读取 `0.CodeMap方法论.md` 与 `1.初级CodeMap制作计划书.md`。
2. 读取 `10.SystemOverview.md`，把需求路由到 Character / Action / Combat / Motion / Locomotion / Camera / Animation 模块。
3. 读取 `11.DependencyGraph.md`，确认允许依赖、五个权威单点、高风险改动表、共享类型和测试入口。
4. 读取相关 `20_Modules/{Module}.md`，至少取得 Purpose、Public API、Dependency、Data Ownership、Forbidden Pattern、Test Strategy。
5. 依据 ModuleMap 中的主路径和类型/方法名，使用 `rg` 定点读取源码；默认范围是 `F:\Code\UnityProject\Core-Drive Action\Assets\GameMain\Scripts`，不得先做全盘递归源码扫描。

## 定点扫描与扩展边界

- 默认只读 CodeMap 指向的模块目录、入口文件、消费者和测试文件；不为建立“完整世界观”读取无关模块。
- CodeMap 没有命中、路径已变更、或现码与地图冲突时，按以下顺序扩大：相关模块目录 → 相邻消费者/测试 → `Scripts` 根内针对符号的 `rg`；仍无法定位时才报告需要更宽范围扫描。
- 禁止把 `F:\Code\Note`、工程盘或整个 Unity 工程作为默认搜索根；禁止用 `Get-ChildItem -Recurse` / 全库 `rg` 代替 CodeMap 路由。
- CodeMap 是静态路由和边界提示，不是运行时证据；现行源码事实优先。冲突必须记录“地图过期/待更新”和实际源码路径。
- 旧 `AI_KnowledgeBase` 仅作为 CodeMap 未覆盖主题的补充索引，不能越过 CodeMap 直接触发全库扫描。

## 施工前影响分析门禁

在修改源码前填写并在 commentary/Output 中保留：

```text
Change:
CodeMap files consulted:
Affected Modules:
Risk / authoritative single point:
Shared types or signatures:
Must not touch:
Migration needed? (yes when Affected Modules >= 3)
Source files to inspect:
Map files to update after landing:
```

`Affected Modules >= 3` 时，先形成 Migration Plan 或使用已有蓝图；不得直接跨模块边搜边改。若入口、依赖方向、数据所有权、禁止项、风险或测试映射发生变化，代码 Landing 同一会话必须回写对应 CodeMap；仅实现细节变化则在 Output 记录“已查 Map、无地图边界变化”。

## 验证与交付

- 每次施工报告列出实际读取的 CodeMap 文件、源码定点路径和未扫描范围。
- 结论区分 CodeMap 既有静态结论、现码静态确认、编译/Test Runner/Play 实测和待验证事项。
- 不因 CodeMap 存在而省略风险相称的编译、测试、探针或人工 Play；CodeMap 只减少无关读取，不替代验证。
