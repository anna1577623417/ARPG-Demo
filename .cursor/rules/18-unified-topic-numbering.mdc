# 一事一号（统一任务标号）

> 用户决议（2026-08-09）：同一件事的全部文档共用统一标号；迭代/修 Bug 默认不起新号，进专区。

## 1. 核心

| 原则 | 含义 |
|---|---|
| **一事一号** | 一件事 / 一项任务对应 **一个任务标号**（Topic ID） |
| **类型不换号** | `【需求】` `【探索问题】` `【决策】` `【蓝图】` `【系统设计】` `【Editor设计】` `【Output】` `【验收指南】` `【验收结论】` `【Log】` 等，**共用同一任务标号**，用中文中括号区分职责 |
| **必要时分级** | 可用二级 / 三级标号拆分同任务内切片，例 `227.3`、`227.3.1`～`227.3.6` |
| **迭代不起新号** | 同主题的迭代、返工、修 Bug：**非用户点名新主题时，禁止领取新主序号**；写入迭代专区或追加原 Output |

## 2. 文件名形态

```text
推荐（同任务多类型共用标号）：
  {任务标号}【类型】{主题}.md
  例：228【需求】… / 228【系统设计】… / 228【Output】…

同任务内切片（Landing / 分册）再用下级号：
  {任务标号}.{切片}【类型】{主题}.md
  例：228.1【蓝图】L1_… / 228.1【Output】…   或  227.3.1【蓝图】…

蓝图状态仍用文件名标记：【TODO】 / 【Done】
```

- **禁止**为「写了一份设计书 / Output / 决策」就自动 +1 主序号。
- **禁止**把类型差异伪装成新主题（例：需求叫 229、设计叫 230）。
- 历史已落的 `228.1【需求】` / `228.10【Output】` 等 **不强制批量改名**；**自本规则生效后的新文件**遵守一事一号。
- `series_files` 仍互链同一任务全家文件；`depends_on` / `source_baselines` 语义不变。

## 3. 何时可以开新标号

仅当满足其一：

1. 用户**显式**要求新主题 / 新主序号；或  
2. 可独立说明与验收的**新事项**（与旧任务目标、边界明显不同），并在序号总表登记。

否则：沿用原任务标号。

## 4. 迭代与 Bug 专区（不起新号）

同任务后续迭代、修 Bug、补丁记录默认落：

```text
06_开发记录/01_开发记录_225起/{十号段}/80_迭代与修复/
  {任务标号}【修复】{简述}.md
  或 {任务标号}【迭代】{简述}.md

问题根因类可同时/改落：
10_问题与失败/02_Bug与根因/
  {任务标号}【Bug】{简述}.md
```

优先做法：

1. 能并入原 `【Output】` 的，用 `---` + `==【n】==` 追加，不新建号。  
2. 需要独立短文时，进 `80_迭代与修复/`，**文件名仍带原任务标号**。  
3. 只有用户点名「新开主题」才进序号总表领新号。

## 5. 与规则 17 的关系

- 显式【需求】仍自动落需求 + 设计书、不自动落蓝图。  
- 落位时的编号改为：**沿用/领取任务标号**，各类型文件共用该号；不再要求「每个类型必须递增不同子序号」。  
- 切片（Landing）才递增 `.1` / `.2` 或三级号。

## 6. AI 自检

- [ ] 本次是否为同一件事却新领了主序号？  
- [ ] 需求/决策/蓝图/Output 是否共用任务标号？  
- [ ] 修 Bug / 小迭代是否误开新号，而非专区或原 Output 追加？  
- [ ] 三级号是否仅用于同任务内必要切片？  
