---
description: Refactor delivery — vertical slice, systemic fixes, no band-aid fallbacks (SkillRoute and all subsystems)
alwaysApply: true
---

# Refactor Delivery Protocol（重构交付 + 禁兜底修 Bug）

> 经验来源：`F:/Code/Note/3.Core-Drive_Action/3D ARPG/06_开发记录/00_旧开发日志_001-224/03_工程内补充开发记录/Refactor/2026-05-17_SkillRoute重构经验教训.md`  
> 与 **`07-single-track-migration.mdc`** 配套：07 禁止双轨；本节禁止「用 fallback 假装修完」。

## 一、Done 的两层定义（强制）

大系统引入**必定伤筋动骨**，需要长链代码落地。Done **不是**一次写完可玩，而是：

> **蓝图规划 → 分 N 次合理落位 → 每次落位后尽快能验证 → 逐步修小 bug / 未握手节点**  
> 前提：**设施（类型、帧序、单轨骨架）已搭好**，差的是 **握手 / 通电**（调用接上、事件贯通、资产绑对）。

### 1.1 单次落位 Done（Landing Done）

**当次 PR / 当次 AI 会话** 完成，须同时满足：

| 条件 | 含义 |
|------|------|
| **设施已就位** | 本切片涉及的类型、单轨调用链、Runtime 工厂、数据 SO 字段 **已存在且编译通过** |
| **握手清单已执行** | 蓝图里本落位负责的 **通电点** 已接线（见 1.3）；未接的须标 `OPEN` 且 **不得** 用 fallback 假装已通 |
| **可观测** | 通电边界有 **固定前缀 Log** 或 `SkillRouteDebug` 类别；跑不起来也能说清断在第几站 |
| **一次落位合理** | 只动 **一层握手面**（例：仅 Semantic→Intent，或仅 Intent→EntryService）；禁止同次改结算+HUD+全资产 |

**Landing Done ≠ 玩法正确**，允许：进 Play Mode、按预期键位、日志走到某一站后断掉。

### 1.2 切片闭环 Done（Slice Done）

经 **N 次 Landing**（蓝图已拆好 N），切片才声称闭环：

- Play Mode **最小验收脚本**通过（3～5 条日志 / 行为）
- 剩余问题限定为：**参数、资产、时序微调** — 不得再改架构或加第二套解析路径

### 1.3 设施 vs 握手 / 通电（规划必填）

每个切片开工前，蓝图或施工表头须有两张表（可合并为一张）：

**A. 设施表**（本切片结束前必须存在）

- 类型 / 接口 / SO / 主循环调用点 / Debug 开关

**B. 握手表**（本落位负责通电的行）

| 通电点 | 上游 | 下游 | 本落位状态 | 验收 Log（通电成功） |
|--------|------|------|------------|----------------------|
| 例：语义入队 | `InputReader` | `InputSemanticResolver` | WIRE / OPEN | `[Semantic] ENQUEUE Tap idx=0` |
| 例：解析落 Route | `SkillEntryService` | `SkillRouteRuntime.OnEnter` | OPEN | `[Resolve] RESOLVED Route=…` |

- **WIRE** = 本 PR 必须接通  
- **OPEN** = 设施在，故意下一落位接；须写清「断点预期 Log」  
- 禁止：设施没建就用 `if (fallback)` 当通电

### 1.4 切片开工四要素（表头）

1. **切片名** + **落位序号** `Landing k/N`（例：`LM Combo`，`2/4`）  
2. **本落位握手行**（仅 B 表中 WIRE 行）  
3. **本落位不做**（避免伤筋动骨扩散）  
4. **git tag** 回滚点  

**禁止**把下列当作任一层的 Done：

- 仅新文件 / Phase 文档 / 能编译  
- 日志有 `RESOLVED` 但 Session/CD/资源语义错误（属 Slice 未闭环，应用握手表定位，不用兜底）

## 二、禁止兜底式 BUG 修复（硬规则）

### 2.1 什么是兜底（禁止作为终态）

以下模式若**没有 accompanying 契约表或单一真相 API**，视为兜底，**不得单独合入**：

| 禁止模式 | 典型后果 |
|----------|----------|
| `if (hasComboRoute) block all Normal fallback` | CD 期填充技、起手语义错误 |
| `EndComboSession` / `ClearPending`  scattered 多处 | 一段完就 CD、Pending 脏数据 |
| Resolver `COERCE` idx 与 Service `REJECT` 打架 | AAABC、无 SESSION START |
| 在 Enter 判定 `isLastSegment` | 首段结束即 Session End |
| `if (resolveFailed) pick Normal anyway` | 连段被 Normal 打断 |
| 新增 `out` 参数但内层重复声明同名局部变量 | CS0136 等编译债 |

### 2.2 系统式修复流程（必须）

1. **先写契约**（表格式，≤1 页）：时间锚点、序号单调、CD/资源结算点、谁写状态谁只读。
2. **单点写状态**：例 — Combo 段位仅 `NotifyRouteEntered` → `CommitAdvance`；窗口锚点仅 `NotifySubRouteSegmentEnded`。
3. **实现**：每个 Inspector 枚举值对应 **唯一** public 入口（如 `ApplyLastSubRouteSettlement`）。
4. **用验收日志对照**，不是「感觉好了」。

### 2.3 允许的临时分支（须标注 TEMP）

仅当用户**明确要求**先跑通演示且书面接受债时，可：

- 加 `// TEMP(slice-xxx):` + Issue 链接 + 删除条件  
- **不得**用 `if (legacy)` / Feature Flag（见 07）

默认：**不允许 TEMP**，必须系统式。

## 三、单轨与「禁止双轨」的执行差异（07 的补充）

### 3.1 反面教材（SkillRoute 迭代真实发生）

- Resolve 阶段仍读 hold 推断 Route → **第二套语义**，已删。
- `UseRouteRuntime`、旧 SkillSystem 与 RouteRuntime 并行 → 已违背 07。
- Inspector 仍显示过时枚举/Tooltip，运行时走新逻辑 → **配置双轨**，人类无法配表。

### 3.2 单轨落地检查表（改 Player 主循环前自检）

- [ ] `grep` 旧类型名在主循环命中数为 0（`SkillDataSO`、`SkillSlotType`、旧 Loadout 读法等）
- [ ] 旧测试已删或改写，不维护两套
- [ ] Editor 下拉与 `RouteCooldownPolicy` / `RouteResourceConsumePolicy` 枚举同步
- [ ] 无 `allowXxxFallback` / `no X fallback` 类日志作为长期逻辑

## 四、垂直切片顺序（Skill 域参考）

默认顺序；用户可覆盖，但**不得**跳步同时开多条线：

1. LM：`Tap → Combo chain → 容器 CD`（无 Charge、无 Entry.Normal）
2. + Entry.Normal **仅容器 CD 期**
3. + Charge 单链路
4. + 其它 Entry / MultiStage / Directional
5. HUD / Presenter / 全量资产迁移

**同一 PR 禁止**：改结算契约 + 改 HUD 样式 + 新 Phase 文档。

## 五、连带面必须在蓝图里点名

Route 重构默认连带（开工清单里逐项勾选）：

- [ ] `*RouteDefinition` Inspector / CustomEditor
- [ ] `SkillEntryService` 帧序与 `PlayerStateManager`
- [ ] `InputSemanticResolver` 与 `SemanticConfigSO`
- [ ] `RouteWidget` / `SkillBar*Presenter` / `IRouteRuntimeHandle`
- [ ] EditMode 测试与 Play Mode 验收日志一致

缺一项 → 该能力**不声称完成**。

## 六、Skill 域时间与结算（与 06 配合）

- **Combo 窗口**：从 **上一 SubRoute 自然结束**（`LastSegmentEndTime`）起算，不是松手、不是 `CommitAdvance`。
- **容器 CD / 资源**：`OnFirstSubRouteStart` 只扣资源；`OnLastSubRouteEnd` 才进 CD；Session 内 SubRoute `SuppressNextCooldown` + `SuppressRouteResourceConsume`。
- **Session 内**：禁止用 per-edge `max` 结束 Session；仅用 `ComboSessionResetTime` 超时。
- **序号**：Session 外只接受 `comboIdx==0`；Session 内只接受 `comboIdx == ComboIndex+1`。

## 七、Unity / C# 工程卫生（减少低级返工）

- `[Tooltip]` / `[SerializeField]` 多行说明：**禁止**内嵌英文 `"` 包裹词组；用 **【】** 或「」标注。
- 新增方法重载时：`grep` 全部调用方 + EditMode 测试。
- 子类需调用的基类方法 → `protected` / `public`，避免 `private` 再改签名。
- 同一作用域不重复声明 `sessionActive` 等变量名（注意嵌套 `if`）。

## 八、可观测与通路修复（跑不起来也要能查）

### 8.1 通电 Log 约定

- **前缀稳定**：与项目 7 层管线一致（例：`[Semantic]` `[Resolve]` `[Combo]` `[Route]`），禁止临时 `Debug.Log("here")`。 
- **断点即报**：握手失败打 **一条** 结构化日志：`[Resolve] OPEN handshake Entry→Route: entry=null` — 含层名、通电点名、关键 null/enum。 
- **与蓝图对照**：握手表「验收 Log」列必须在实现前就写好；落位结束对照 **出现 / 未出现**。 
- 优先复用 `SkillRouteDebug` / 既有 `Cat*` 分类，不新增散落开关。

### 8.4 新笔记工作区的 Log 与施工记录目录

| 路径 | 用途 |
|------|------|
| `11_测试与验收/06_Log与运行时探针/{编号段}/` | **只读原始区**：用户粘贴 Play Log；AI 禁止改写 |
| `06_开发记录/01_开发记录_225起/{编号段}/` | AI 验收对照、施工记录和 Output |
| `05_代码落位蓝图/{编号段}/` | 当前蓝图；从 `225-230` 起按主序号分段 |

- **非必要**不写冗长总结 Log；对照用户原始日志时，在开发记录中写简短 **【Output】**，验收结论独立写入 `11_测试与验收/02_验收结论/`。

### 8.2 通路修复顺序（禁止跳步兜底）

1. 对照握手表：最后一行 **成功 Log** 停在哪一站。  
2. 只修该站 **上游→下游** 接线或数据（资产引用、帧序、事件订阅）。  
3. 仍失败 → 查契约（时间/序号/结算）是否与本站相关；**不**先改其它层 fallback。  
4. 修完补一条 EditMode 或 Play 验收 Log（可进切片回归清单）。

### 8.3 代码质量（为定位服务）

- 单轨主路径 **无 silent return**（早退必 Log 或 `SkillRouteDebug`）。  
- 状态变更集中在契约规定的 **单点 API**；其它层只读。  
- 新增类型放在蓝图规定的层目录；**一次落位不跨层堆 God 方法**。

## 九、蓝图 → 落位规划（引入系统前必做）

1. **读蓝图** → 拆 **N 个 Landing**（每个仅 1～3 个 WIRE 握手点）。  
2. **先设施后通电**：Landing 1 可只有类型+空壳 Tick+Log；Landing 2 接 Input→Semantic；…  
3. **最早验证点**：蓝图须标 **Landing 几可首次进 Play Mode**（通常 ≤2，不得拖到全部写完）。  
4. **Slice 验收**写在蓝图末尾，与握手表最后一行的 Log 一致。

**同一迭代 WIP**：1 个切片 + 当前 `Landing k/N`；不得并行第二个切片的 WIRE。

## 十、AI 输出顺序（重构类任务）

1. 契约表（时间 / 状态 / 结算 — 仅当本落位涉及）  
2. **设施表 + 握手表**（含 WIRE/OPEN、验收 Log）  
3. 本落位删改文件列表（单握手面）  
4. 实现（边界 Log 与表同步落地）  
5. **通电对照**：预期 Log / 实际断点 / 下一落位 OPEN 行  

**禁止**：先堆全量实现再补 Log；禁止无握手表开工。
