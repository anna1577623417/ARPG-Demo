---
description: All AI-produced Markdown/docs must stamp date-time on line 1 of body
alwaysApply: true
---

# 文档产出时间戳（正文首行）

凡由 AI **新建或实质性更新**并写入仓库的 Markdown / 施工日志 / 蓝图 / 分析报告，**正文第一行**必须是时间戳（精确到**小时:分钟**），不得从第二行或标题之后才开始写时间。

## 格式（强制）

```markdown
> 产出时间：YYYY-MM-DD HH:mm
```

- `YYYY-MM-DD`：四位年、两位月、两位日（补零）
- `HH:mm`：24 小时制，补零（例 `09:05`、`18:30`）
- 使用编写者所在**本地时区**的墙钟时间；若上下文已给出时区（如 UTC+8），与该时区一致即可，**不必**在首行写时区后缀

## 适用范围

- `Assets/AI Agent/Output/**`
- `Assets/GameMain/Scripts/6.AI/Cursor/Output/**`
- `Assets/GameMain/Scripts/6.AI/Cursor/BluePrint/**`
- `Assets/GameMain/Scripts/6.AI/Cursor/Log/Record/Output/**`（AI 落盘；`Log/Test/**` 为用户只读原始 Log，不适用本规则）
- 其它由任务明确要求落盘的 `.md` 分析 / 评审 / 版本总结 / 施工日志

**不适用**：源码（`.cs` 等）、Unity 资产（`.asset`）、仅对话内回复不落盘的文字。

## 新建 vs 更新

| 场景 | 首行 |
|------|------|
| **新建** | `> 产出时间：YYYY-MM-DD HH:mm` |
| **实质性更新**（增删章节、改结论、改施工记录） | 第一行改为 `> 最后更新：YYYY-MM-DD HH:mm`；若需保留首次产出，第二行写 `> 产出时间：…`（原文时间） |

标题（`# …`）从**第二行**起；首行只能是时间戳一行，前面不要空行、不要 YAML front matter 挡在时间戳前。

## 示例

```markdown
> 产出时间：2026-05-16 18:30

# 105.4 单轨施工日志

…
```

## 自检

落盘前确认：打开文件 → 第 1 行匹配 `> 产出时间：` 或 `> 最后更新：` + 合法日期时间。
