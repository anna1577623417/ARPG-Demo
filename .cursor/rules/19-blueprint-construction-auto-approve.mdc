---
description: 蓝图施工 / 文件落位 / 点名删除 / 配套脚本 — 零询问 Auto-Approve
alwaysApply: true
---

# 蓝图施工 Auto-Approve（零询问）

用户决议（2026-08-09）：下列施工动作 **一律直接执行，不得再询问「是否继续 / 是否删除 / 是否运行脚本」**。

## 覆盖范围（自动批准）

1. **蓝图代码施工**：按已确认蓝图 / Landing 改 C#、Editor、诊断开关等生产代码。  
2. **文件落位**：新建/修改笔记 Output、蓝图状态改名、Hub / 模块进程、`.meta`、施工所需本地脚本。  
3. **指明删除**：用户点名路径，或当前蓝图明确要求删除的文件 / 目录 / 配套脚本产物。  
4. **配套脚本与终端**：为上述施工服务的 Python / PowerShell（`.py` / `.ps1` 及等价 `Shell` 命令）、批量改名、生成 meta、静态核对。

## 行为要求

- 进入蓝图施工后：**先做后报**；进度可用短句同步，但不阻塞等待「继续」。  
- 删除仅限 **已指明** 或蓝图清单内目标；删除后在 Output 列出路径。  
- 脚本失败则修脚本或改用等价命令继续，不改为口头请示是否运行。

## 仍须停下的例外（不在本批准内）

| 例外 | 说明 |
|---|---|
| 未点名改 `.asset` / `.prefab` 业务配置 | 见规则 14 |
| 用户未要求的 `git commit` / `push` / 强制回滚 | 见用户 git 规则 |
| 未点名的全库 Migrate / 大规模无关删除 | 见规则 12 |
| 改变蓝图目标或二选一架构分叉 | 问唯一必要问题 |
| 需要 Unity Editor 内纯手动拖拽且代码无法替代 | 报告阻塞 |

## 与 Cursor User Rule 的关系

同内容已写入 Cursor 个人规则「蓝图施工 Auto-Approve」。本文件为仓库 alwaysApply，保证本工程 Agent 会话一致生效。
