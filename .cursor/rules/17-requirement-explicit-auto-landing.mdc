---
description: 显式【需求】自动落位需求说明与设计书，不自动落蓝图；目录与双属性多落位规则
alwaysApply: true
---

# 显式需求 → 自动落位（需求 + 设计，不落蓝图）

> 笔记根目录：`F:\Code\Note\3.Core-Drive_Action\3D ARPG`  
> 目录权威以该根下 `TREE.md` 为准。大类文件夹 **`01_需求与迭代`**（原名 `01_需求与范围` 已弃用）。

## 1. 触发条件

用户消息**显式**出现下列之一时，视为「标明需求」，必须执行本节自动落位：

- 标题或正文含 `【需求】`
- 明确写出「新需求」「需求：」「需求描述」等同等意图

未显式标明时：不要仅为闲聊或技术讨论擅自新建需求主序号文档。

## 2. 自动落什么 / 不落什么

| 文档 | 是否自动落位 |
|---|---|
| `【需求】`（当前阶段） | **必须** |
| 双属性需要时的 `【需求池】` 等副本 | **按目录树多落** |
| `【系统设计】` / 必要时 `【Editor设计】` 等设计书 | **必须**（与需求同轮） |
| `【决策】`（若改变推荐路径/范围） | 建议同轮 |
| `05_代码落位蓝图/**` 下蓝图 / Landing | **禁止自动落位** |
| Unity 生产代码 / `.asset` | **禁止**仅因需求描述而改（除非用户另开施工指令） |

原因：设计书须人工核查是否符合期望；蓝图是施工合同，只能在设计确认后由用户授权再写。

## 3. 落点目录（225+）

```text
需求权威：
 01_需求与迭代/03_当前阶段需求/{任务标号}【需求】{主题}.md

需求池双属性（可选但推荐）：
 01_需求与迭代/02_需求池/{任务标号}【需求池】{主题}.md

设计书（按属性选目录，可多份）：
  04_设计说明书/03_数据与API设计/{任务标号}【系统设计】{主题}.md
  04_设计说明书/05_Editor与配置化设计/{任务标号}【Editor设计】{主题}.md
 （其它 01/02/04/06 子目录按真实属性选用）

决策（若有）：
  09_决策记录/03_范围与优先级决策/{任务标号}【决策】{主题}.md
```

编号（与规则 18「一事一号」对齐）：

- **任务标号**领取/延续自 `00_Project-Hub/02_开发序号总表_225起.md`。
- 同一件事的需求 / 探索问题 / 决策 / 设计 / 蓝图 / Output **共用同一任务标号**；用 `【类型】` 区分职责（例 `228【需求】`、`228【系统设计】`、`228【Output】`）。
- 仅当同任务内需要切片（Landing / 分册）时，才用二级或三级号（例 `228.1`、`227.3.1`）。
- **禁止**为新文档类型自动 +1 主序号；迭代 / 修 Bug 默认进 `80_迭代与修复/` 或追加原 Output，非点名不起新号（见 `18-unified-topic-numbering.mdc`）。
- **蓝图状态**：文件留在原十号段目录；用文件名 `【TODO】` / `【Done】` 标记。禁止使用 `80_暂停蓝图` / `90_已完成蓝图` 文件夹搬迁。

## 4. 双属性与多落位

- 同一逻辑主题可有**不同中文中括号类型**文件名：`【需求】` / `【需求池】` / `【系统设计】` / `【Editor设计】` / `【决策】` …
- 同一主题也可落在**不同子文件夹**（双属性）：例如 API 设计 + Editor 设计各一份。
- 必须用 YAML `series_files` + 正文「同系列文件导航」互链。
- 双属性副本须标明：哪一份是权威正文；其它副本写「索引/视角副本，权威见 …」，避免双真相长期分叉。
- 禁止把蓝图目录当作需求/设计的双属性落点。

## 5. 同轮最少动作

1. 写/更新需求与设计书（及声明的双属性副本）。
2. 更新 `00_Project-Hub/01_当前进度与下一步.md` 与序号总表（状态写清：设计待核查、蓝图未建）。
3. 若影响模块，更新对应 `07_模块开发记录/**/03_开发进程.md`，并标明「文档 only」。
4. 在回复中列出：新建路径、请用户核查的设计焦点、**明确写「蓝图未建，确认设计后再开」**。

## 6. 与其它规则的关系

- 时间戳 / YAML：`08-document-output-timestamp.mdc`、`AGENTS.md`
- 蓝图协议：仅在用户确认设计并要求写蓝图后启用 `16-blueprint-code-landing-architect.mdc`
- 代码师傅 / 单轨 / 禁暗改 SO：设计未确认前不进入代码 Landing
- 笔记侧同步说明：`06_开发记录/01_开发记录_225起/00_日常开发落位工作流.md`
- 编号：`18-unified-topic-numbering.mdc`（一事一号；本规则落位时必须遵守）
