> 产出时间：2026-05-31 21:30

# 136.1 施工日志 — Landing 1～4（四向 Group + Loadout 装配）

## 切片状态

| Landing | 状态 | 说明 |
|---|---|---|
| L1 Loadout 装配 | **WIRE Done** | `SkillEntryLoadoutSO` 增 `combatFlow` / `contextGroups` / `abilityMap`；`Rebuild` → `AttachFromLoadout` |
| L2 Directional 可观测 | **WIRE Done** | 既有 `[SkillRoute][Dodge4] Semantic ENQUEUE` 链保留 |
| L3 ContextGroup + Group | **WIRE Done** | `SkillGroupDefinition.SelectByDirection`；Directional 禁止 Graph/Normal 回退 |
| L4 Ability MVP | **WIRE Done** | `AbilityGateService` 在 Directional 解析前检查 `Ability.Mobility` |
| L5～L8 | **OPEN** | 打断窗口 / CombatFlow Editor / 删 DirectionalRoute 类型 / 全量回归 |

## 本落位改动的文件

### 数据层
- `SkillEntryLoadoutSO.cs` — Loadout 挂载 CombatFlow / ContextGroups / AbilityMap
- `SkillGroupDefinition.cs` — 组内四向槽位 + `SelectByDirection`
- `SkillContextGroupDefinition.cs` — 上下文 → Group 映射（新）
- `AbilitySemantic.cs` / `AbilityGateRuleSO.cs` / `AbilityMapSO.cs` — Ability 闸门设施（新）

### 运行时
- `SkillEntryService.cs` — 单轨解析：`PrimaryGroup/ContextGroup` → 四向；Directional `NO_ROUTE` 显式拒绝
- `CombatGraphRuntime.cs` — 多候选边 + `CanCast=false` 继续尝试
- `AbilityGateService.cs` — Loadout.AbilityMap 准入（新）
- `Player.cs` — 移除 `combatFlowGraph` 字段；Flow 从 Loadout 注入

### 资产（C1 默认）
- `Group_Standing_Dodge.asset` — F/B/L/R 四 Route 已填
- `Entry_Space.asset` — `primaryUnit=Group`；`normalRoute` 清空
- `Loadout_Skill_C1.asset` — `combatFlow=Combat_Graph_4dir_Roll_Space`

### 编辑器 / 测试
- `SkillGroupStandingDodgeAuthoring.cs` — 菜单 `Tools/Skill/136.1 Setup Standing Dodge SkillGroup (4 dir)`
- `SkillEntryRouteResolveTests.cs` — 四向 chord 选取 + 禁止 Normal 回退

## 通电对照（Play Mode，`Player.DebugSkillRoute=true`）

| 步骤 | 预期 Log | 状态 |
|---|---|---|
| 进 Play | `[SkillRoute][Setup] loadout flow=Combat_Graph_4dir_Roll_Space abilityMap=null contextGroups=0` | 待人工验证 |
| Space+W | `[SkillRoute][Dodge4] Unit \| PICK Group=Group_Standing_Dodge child=Route_Normal_Standing Dodge Forward chord=Forward` | 待人工验证 |
| Space+S/A/D | 对应 Backward/Left/Right child | 待人工验证 |
| Group CD 中再按 | `SKIP Group=... CanCast=false` → `NO_ROUTE (Directional)`（**不是** NormalRoute） | 待人工验证 |

## 解析主链（终态）

```
InputSemantic(Directional + axis)
  → AbilityGateService(Mobility)     [L4]
  → ContextGroup（Loadout，可选）    [L3]
  → SkillGroupDefinition.SelectByDirection
  → RouteRuntime
```

Directional **不再**走：`entry.DirectionalRoute` / `CombatGraph` 同层选路 / `Entry.NormalRoute`。

## 下一落位（L5）

- ~~`ActionInterruptResolver` + Ability 闸门~~ → 见 `136.1【Log】L5-L8打断_Editor_单轨回归.md`

## 回滚

```bash
git tag skill-v136.1-L4   # 建议在本 PR 合并前打 tag
```
