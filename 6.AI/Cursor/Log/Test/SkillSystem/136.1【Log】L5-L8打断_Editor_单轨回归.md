> 产出时间：2026-05-31 22:15

# 136.1 施工日志 — Landing 5～8（打断 / Editor / 单轨切断 / 回归）

## 切片状态

| Landing | 状态 | 说明 |
|---|---|---|
| L5 打断 Ability 化 | **WIRE Done** | `ActionWindow.incomingAbilityGate` + `ActionInterruptResolver.TryPassIncomingAbilityGate` |
| L6 CombatFlow Editor | **WIRE Done** | `CombatGraphAssetEditor` 边条件/优先级/Always 警告 |
| L7 删 DirectionalRoute 链 | **WIRE Done** | 运行时不再注册/解析；`RouteResolver` 移除；迁移菜单 |
| L8 资产收口 + 回归 | **WIRE Done** | C1 Loadout 绑 AbilityMap/ContextGroup；EditMode 测试 |

## L5 — 打断窗口

- `ActionWindow.incomingAbilityGate`：非 None 时覆盖默认类别→Ability 映射
- `ActionInterruptResolver.CanInterrupt(..., Player player)`：窗口通过后查 `AbilityGateService`
- `PlayerActionState` 传入 `player`；`DebugInterruptFlow` 时输出 `[Interrupt] allowByAbility` / `deny by ability`

## L6 — CombatFlow Inspector

- `Editor/Inspectors/CombatGraphAssetEditor.cs`
- 展示每条边的 Trigger / Conditions / Route / priority
- Directional + `Always` 条件 → Warning

## L7 — 单轨切断

- `SkillEntryService.RegisterEntry` 不注册 `DirectionalRouteSet`
- `SkillEntryDefinition.CollectAllRoutes` 不再收集 directionalRoute
- `RouteResolver` 删除 Directional 分支
- `SkillEntryDefinition.DirectionalRoute` 标记 `[Obsolete]`（迁移只读）
- 菜单：`Tools/Skill/136.1 Migrate DirectionalRouteSet → Group (All)`

## L8 — 默认资产

| 资产 | 用途 |
|---|---|
| `AbilityGateRule_MobilityGrounded` | Space 翻滚仅接地可施放 |
| `AbilityMap_C1` | Space + Mobility → 上规则 |
| `ContextGroup_StandingDodge_Space` | Directional+Grounded → Group_Standing_Dodge |
| `Loadout_Skill_C1` | 引用上述 + combatFlow |

菜单：`Tools/Skill/136.1 Setup C1 Loadout (Ability+Context+Flow)`  
Loadout Inspector 按钮：`136.1 一键装配 C1 Space 翻滚`

## EditMode 回归（新增）

- `DirectionalIntent_PicksGroupChildByChord`
- `DirectionalIntent_DoesNotFallbackToNormalRoute`
- `AbilityGate_DeniesMobilityWhenGroundedBlocked`
- `DirectionalIntent_AbilityGateDeniesViaLoadout`
- `InterruptAbilityGate_BlocksWhenLoadoutDenies`

## Play Mode 验收脚本（136.1 §八）

1. `Space+W/S/A/D` → 四向 Route（`[SkillRoute][Dodge4] Unit PICK Group=...`）
2. `[SkillRoute][Setup] loadout flow=... abilityMap=AbilityMap_C1 contextGroups=1`
3. 滞空时 Space+方向 → `[Ability] gate deny` + `NO_ROUTE (Directional)`
4. 动作后摇窗口内 Space+方向：`DebugInterruptFlow` → `[Interrupt] allowByAbility=true` 或 `deny by ability`

## 136.1 Slice Done 判定

- 四向主链：`ContextGroup/PrimaryGroup → Group.SelectByDirection` ✓
- Directional 禁止 Normal/Graph 同层回退 ✓
- CombatFlow 挂 Loadout ✓
- Ability 闸门：Resolve + Interrupt ✓
- 运行时主路径无 `DirectionalRouteSet` 解析 ✓

## 后续（137/138 蓝图，非本切片）

- 137.1：StateTag / 打断窗口 / CombatFlow 与 interrupt 分责深化
- 138.1：AbilitySystem 滞空阶段（Ascending/Falling）闸门
