> 产出时间：2026-05-31 16:00

# 147.1 Landing 4 — Condition 资产化 + 边 Conditions 面板

## 设施

| 类型 | 路径 |
|---|---|
| `CombatFlowConditionDefinition` | 可复用 Condition SO |
| `CombatGraphAsset.conditionPool` | Condition 资源池 |
| `CombatFlowEdgeAuthoring.ConditionRefs` | 边引用资产（与内联 AND） |
| `CombatFlowConditionMerge` | 编译期扁平化 |
| Graph 边面板 | Inline + ConditionRefs + 合并预览 |

## 工作流

1. 创建 `FlowCondition_*.asset`（如 Grounded）
2. 加入 Graph **conditionPool**
3. Graph 编辑器选中边 → **Conditions** 区挂 Ref 或内联
4. **Sync && Compile**

## 清理

- 删除 `CombatFlowMvpGenerator` 及全部 `Tools/SkillRoute/147.1 Setup*` / `Compile All` 菜单
- 删除 `CombatFlowLanding2/3Tests`
- 新 Rule：`.cursor/rules/12-no-unrequested-scaffolding.mdc`

## 仍 OPEN（后续）

- SelectorNode / SubGraph / InterruptNode / AutoLayout
