> 产出时间：2026-06-05 16:20

# 149.3 Landing 5~7 施工记录

## L5 — Air / Cancel 双闸门

| 项 | 实现 |
|---|---|
| Cancel Log | `LastIntentResolvedViaGraph` + `PlayerActionState` → `[Flow] CANCEL via interrupt cat=… → graph edge→…` |
| Air Context | 边 `OnAirborne()` / `OnGrounded()`（已有 `ConditionEvaluator`）；RESOLVE 日志带 `(airborne)` |
| MultiStage 游标 | `ObserveStageChangeAndNotifyPresentation` → `BindEntryAction` 段切换同步 |

## L6 — 编辑器

| 项 | 实现 |
|---|---|
| Early 只读投影 | `CombatFlowGraphEdgeInspector.DrawEarlyWindowProjection` — From 节点 Action 的 Interrupt 窗 + 绝对秒 |
| Late 可编辑 | 已有 `Late Window (s)` 字段 |
| Loadout Inspector | `GraphEnabled` / `MissPolicy` 显示 + 149.3 HelpBox |

## L7 — 清债与文档

- `grep`：`TryAdvanceCombatFlowOnInput` / `useGraph` / `allowXxxFallback` → 0（`.cs`）
- `CombatFlowGraphValidator.ValidateGraphCycles` — 环仅 Warning（Loop Combo 允许）
- 更新 `134.1【操作指南】` 帧序与 SlideExit 路径说明

## Play 验收（§8 八项 — 需人工）

1. `Loadout_Skill_C1.combatFlow` 绑定已编译 Graph
2. 开 `DebugSkillRoute`
3. 对照蓝图 §8 表逐条 Log

| 验收项 | 预期 Log |
|---|---|
| LM-LM-LM | `RESOLVE node=A1 in=LM → edge→…` |
| A1-A2-B1 | `node=A2 in=RM → edge→B1` |
| Idle RM ≠ Combo RM | `node=Start in=RM` vs `node=A2 in=RM` |
| Air Combo | `RESOLVE … (airborne)` |
| Dodge Cancel | `CANCEL via interrupt cat=Movement → graph edge→…` |
| Launcher 链 | 段切换 `BindEntryAction` + OnSegmentComplete |
| Late Window | `LATE open` / `LATE consume` |
| Graph Fallback | `MISS policy=Fallback→Entry` 或 `policy=Block discard` |
