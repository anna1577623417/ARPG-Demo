> 产出时间：2026-06-05 15:55

# 149.3 CombatFlow 重构 — Landing 0~4 施工记录

## 本落位范围

| Landing | 状态 | 说明 |
|---|---|---|
| L0 设施 | ✅ | `GraphEnabled` / `IsEnabled` |
| L1 Graph 优先 | ✅ | `TryResolveContextual` + 删除 `TryAdvanceCombatFlowOnInput` 旁路 |
| L2 Start=Idle | ✅ | `GetResolutionNodeId`：无 ActiveRoute → `StartNodeId` |
| L3 MissPolicy | ✅ | `CombatFlowGraphMissPolicy` + Block 吞输入 |
| L4 Late Window | ✅ | `LateWindowSeconds` + `LATE open/expire` |
| L5 Air/Cancel | OPEN | 依赖图配置 + 现有 Interrupt；无专用 Cancel 后查图 |
| L6 编辑器 Early 投影 | OPEN | 仅加 Late 字段编辑 |
| L7 验收矩阵 | OPEN | 需绑 Loadout + 配 C1 验收图 |

## 删改文件

- `CombatGraphRunner.cs` — `TryResolveContextual`、Late Window、`GetResolutionNodeId`
- `SkillEntryService.cs` — Graph 优先解析、MissPolicy Block、`TickLateWindow`
- `PlayerStateManager.cs` — 移除二次查图
- `CombatGraphAsset.cs` / `CombatFlowAuthoring.cs` / `CombatFlowData.cs` — MissPolicy、LateWindowSeconds
- `CombatFlowGraphCompiler.cs` / `CombatFlowGraphSync.cs` / `CombatFlowGraphEdgeInspector.cs`
- `CombatGraphAssetEditor.cs` — 文案 149.3

## 握手表通电（预期 Log）

| 通电点 | 验收 Log |
|---|---|
| H1 Graph 优先 | `[Flow] RESOLVE node=A1 in=LM → edge→Route_...` |
| H2 无旁路 | `grep TryAdvanceCombatFlowOnInput` → 0 |
| H3 Start Idle | `[Flow] RESOLVE node=Start in=RM → ...` |
| H5 Miss Block | `[Flow] MISS policy=Block in=Space node=A2 discard` |
| H6 Late | `[Flow] LATE open 0.25s node=A3` → `LATE consume` / `LATE expire` |

## Play 验收步骤（最小）

1. `Loadout_Skill_C1.combatFlow` → `CombatFlow_Player`（或验收专用图）
2. Graph：Start 出边 + A1→A2→A3（OnInput=LM）+ A2 分支 RM→B1
3. Inspector **Validate && Compile** → `compileValid=true`
4. Player `DebugSkillRoute` 开启
5. 验证 LM-LM-LM、A2+RM→B1、Idle RM vs Combo RM（Start vs A2 节点）

## OPEN（下一落位）

- L6：边面板 Early Window 只读投影（ActionWindow）
- L5：Cancel 后若需「仅 Interrupt 通过再读 Graph 目标」可再收紧帧序
- MultiStage 段内游标同步（149.1 已知限制）：AutoInRoute 不在段间推 Flow 指针
