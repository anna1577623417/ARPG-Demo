> 产出时间：2026-05-31 15:00

# 147.1 Landing 3 — AllowFlowSegmentAdvance + 边属性面板 + 移除 Legacy

## 契约（B 开关）

| 场景 | 权威 |
|---|---|
| 地面 LM 连段 | `ComboRoute` + Resolve + link window（默认） |
| Flow `OnSegmentComplete` → Combo 子 Route | 须 `ComboRouteDefinition.AllowFlowSegmentAdvance = true` |
| Flow `OnInput` / 非 Combo 子 Route | 不受 B 开关影响 |

## 本落位 WIRE

| 通电点 | 状态 | 验收 |
|---|---|---|
| `CanFlowSegmentAdvanceTo` 运行时闸门 | WIRE | `[Flow] SKIP … AllowFlowSegmentAdvance=false` |
| Validator 编译前拦截非法边 | WIRE | Validate INVALID + E: flowEdges… |
| Graph 边属性面板 | WIRE | 选中边 → Transition / TargetRoute |
| 移除 124.1 Legacy 输入边 | DONE | 无 `CombatGraphLegacyEdge` / 无迁移菜单 |

## 删除（旧 CombatFlow）

- `CombatGraphLegacyEdge` + `legacyInputEdges`
- `MigrateLegacyInputEdges` + Inspector 迁移区
- C1 默认模板中的 Auto-A→B 边

## 演示

- 标准 C1：`Tools/SkillRoute/147.1 Setup C1 Combat Flow (Template)` — 仅 RM 派生 Flow 边
- 自动连段演示：`Tools/SkillRoute/147.1 Setup C1 Flow Auto-Combo Demo` — 开 B 开关 + Auto-A→B

## Play 验收

1. **默认 C1**：LM A→B 仍须窗内再按 LM；无 `SegmentComplete→Enter`
2. **Auto-Combo Demo**：A 播完自动 B；日志 `ComboHandoff` + 无重复 `LINK WINDOW OPEN`

## EditMode

- `CombatFlowLanding3Tests`

## 147.1 完全体状态

| 层 | 状态 |
|---|---|
| 资产 pools + nodes + flowEdges + compile | ✅ |
| GraphProcessor 画布 + 边面板 + Sync | ✅ |
| Runner 只读 CompiledData | ✅ |
| Entry/Route 入口 / Flow 段后+段内 | ✅ |
| B 开关 Combo 段后策略 | ✅ |
| Legacy 124.1 输入边 | ❌ 已移除 |

未做（后续）：Selector / SubGraph / Condition 资产化 / InterruptNode / AutoLayout
