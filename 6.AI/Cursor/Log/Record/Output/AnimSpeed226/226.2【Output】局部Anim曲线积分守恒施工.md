> 产出时间：2026-08-09 15:10

# 226.2 Output：局部 Anim 曲线积分守恒施工

讲解字数计划：约 1000 字（代码正文不计入）。

适配蓝图：226（M0 → L1/L2/L3 合并施工）  
非法曲线策略（用户确认）：**策略1 — Editor 红字拒绝写入；Runtime 拒绝非法曲线（factor=1）并 `[AnimSpeed226] REJECT`**

---

==【1】==

## 本次 Landing

- **范围**：L1 Authority 去门控 + L2 积分/三点数学 + L3 竖排 Inspector；点名资产 Sword Dash RM MP 调至守恒三点。
- **不做**：全库曲线迁移；StopAuthoring/InheritPhysics 重做；新增 Setup 菜单。

## 代码编写过程

1. 抽出 `AnimSpeedIntegralMath`：三点积分、曲线梯形积分、`TrySolveThirdPoint`、`BuildThreePointCurve`。
2. `ActionAnimSpeedAuthority` 删除 Free 门控；非法 ∫ 拒绝为 1；`clipDone`/`Preview` 改积分推演。
3. `MotionProfileSO` 增加 AuthoringMode / SolveTarget / 三点字段与校验 API。
4. `MotionProfileEditor` 竖向单列 UI：Constant / Freehand / ThreePointConserve；非法红字 Error HelpBox。
5. 更新既有 EditMode 断言；点名改 `General_Armature_Sword_Dash_RM_MotionProfile`：`0.5 / 0.8 / 1.9`（I=1）。

## 本次实现了哪些功能

- AutoFit 与 Free 均可叠加合法 SpeedOverTime。
- 三点锁二求一（默认求 End）。
- 非法曲线：Inspector 红色警告；Runtime 不采用。
- Sword Dash RM 专项曲线已守恒。

## 代码布局与用途

| 布局 | 用途 |
|---|---|
| `4_Data/3.Motion/AnimSpeedIntegralMath*` | 数学与求解单一真相 |
| `ActionAnimSpeedAuthority` | S × f 合成与拒绝门禁 |
| `MotionProfileEditor.DrawAnimationSpeedSection` | 竖排作者 UI |
| Sword Dash MP.asset | 垂直切片验收资产 |

## 运行时数据流

```text
Action 基准 S（AutoFit|Free）
  → profile.IsAnimSpeedCurveIntegralValid?
       否 → factor=1 + [AnimSpeed226] REJECT
       是 → f(motionT)=SpeedOverTime
  → final = S·f → MotionExecutor.SetSpeed
  → 位移仍跟 motionT
```

## 与蓝图差异

- L0 独立基线 Log 未在本环境 Play；标 OPEN。
- Note 工作区本机路径不可用，Output 暂落 `6.AI/Cursor/Log/Record/Output/AnimSpeed226/`。

## 验证结果

- 静态：门控删除、三点公式、资产 I=1 已写入。
- EditMode：已改断言（需本机 Unity Test Runner）。
- PlayMode / 实机：OPEN。

## 资产变更清单（用户点名）

| 路径 | 字段 | before → after |
|---|---|---|
| `.../General_Armature_Sword_Dash_RM_MotionProfile.asset` | SpeedOverTime End | 1.2 → **1.9** |
| 同上 | MidTime | 0.49 → **0.5** |
| 同上 | Authoring | （无）→ ThreePointConserve + Start0.5 Mid0.8 End1.9 |

## 未完成门禁

1. Unity 编译 + EditMode。
2. Play Sword Dash：确认 `profileFactor` 非恒 1，Duration 结束无滑步。
3. 将本 Output 同步回 Note 工作区 226 系列（环境存在后）。

## 本次学习内容

1. 基准 S 与形状 f 正交。
2. 积分守恒是防滑步的边界条件。
3. 策略1：非法不假装可用。
4. Inspector 竖排降低曲线区被横向拉扁的误操作。

## 手写复现

纸上：`a=0.5,b=0.8 → c=1.9`；说明为何 AutoFit 下仍要 ∫f=1。

## 3A 模板检查

- 通用 Action×MP 能力，非角色硬编码。
- 节奏差异落在 MP 三点/曲线配置。
