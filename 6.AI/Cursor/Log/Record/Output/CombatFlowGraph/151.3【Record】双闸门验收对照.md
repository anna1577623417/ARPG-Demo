> 产出时间：2026-06-05 18:00

# 151.3 双闸门验收对照

**原始 Log 来源（只读）**：`6.AI/Cursor/Log/Test/CombatFlowGraph/151.3【Log】双闸门Log.md`

## 结论

**符合设想**：Combat Graph 已启用，Flow 未正确配置（`Slide_Exit` 节点 / RM 出边缺失），**Exit 段按 RM 的打断意图被阻挡**，并打出 `DUAL_GATE block reason=graph-miss`。

## 关键行对照

| 行 | Log | 含义 |
|----|-----|------|
| L6–L7 | `MISS node=Action in=RM` → `DUAL_GATE block … reason=graph-miss` | 动作进行中、当前游标在 `Action` 节点，无 RM 出边 → **禁止回落 Entry**，剑冲未解析 |
| L5 | `BindEntryAction …Slide_Loop… node End→Action` | 多阶段 Auto 推进；游标跟到通用 `Action` 节点（非 `Slide_Exit`） |
| L9 | `BindEntryAction OPEN …Slide_Exit… stay=Action` | 图内无 `Slide_Exit` FlowAction 节点，末段无法对齐游标 |
| L1–L2 | `MISS node=Start in=Shift` → `Fallback→Entry` | **空闲起手**仍允许回落 Entry（双闸门仅约束「动作进行中」） |

## 未出现（本场景正常）

- `DUAL_GATE pass` — 图未配边，不应出现。
- `DUAL_GATE block reason=early-window` — Resolve 阶段已 `graph-miss` 拦截，未进入 Early 窗口判定。

## 资产待修（非代码缺口）

1. 新增 Flow Action 节点 `Slide_Exit`，Action = `General_Armature_Slide_Exit_ActionData`。
2. 从 `Slide_Exit` 拉 **OnInput(RM)** 边，`TargetRoute = Route_Normal_Sword_Dash_RM`。
3. 配好后期望：`BindEntryAction …→Slide_Exit`，按 RM 时出现 `DUAL_GATE pass` 或 `RESOLVE node=Slide_Exit in=RM`。
