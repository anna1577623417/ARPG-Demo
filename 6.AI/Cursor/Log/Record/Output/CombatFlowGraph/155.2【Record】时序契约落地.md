> 产出时间：2026-06-07 19:15

# 155.2 时序契约落地记录

## 已落位（Landing 1–4）

| Landing | 改动 | 文件 |
|---|---|---|
| **1/4** | Resolve 不再写 `_currentNodeId`；`PendingGraphTransition` + `FLOW/INTERRUPT pending`；游标仅在 `BindEntryAction`（Flow）或 Interrupt pending→End | `CombatGraphRunner.cs` |
| **2/4** | `GetResolutionNodeId` 优先 `ActiveRoute.Stage.Action` 对应节点；不一致打 `[ComboChain] CURSOR_LAG` | `CombatGraphRunner.cs`, `CombatGraphComboChainDiagnostics.cs` |
| **3/4** | `NotifyRouteNaturalExit(exitingAction)`；Late 锚点用段结束 Action 节点 | `CombatGraphRunner.cs`, `SkillEntryService.cs` |
| **4/4** | 纯 `OnSegmentComplete/Immediate` 环 → **Error**；含 OnInput 环 → Warning | `CombatFlowGraphValidator.cs` |

## Play 验收（对照 154.1）

1. 连点 LM ABC：每段 `RESOLVE node=<在播段>` → `BindEntryAction` → 下一段 RESOLVE；**不应**出现 Resolve 前的 `FLOW advance cursor`。
2. 快速连点：不应 `RESOLVE A→B` 紧接 `RESOLVE B→C` 且无中间 `BindEntryAction Combo_B`。
3. 段末：Late 应 `LATE open … node=ComboA`（刚结束段），而非未来段。
4. Validator：Slide 纯 SegComplete 环应报 Error；LM 连招环仅 Warning。

## OPEN

- Intent 缓冲层若未来存 Route/Node，须单独评审（铁律 5）。
- 数据侧：A→B/B→C 边 `LateWindowSeconds=0` 仍为体感微调项。
