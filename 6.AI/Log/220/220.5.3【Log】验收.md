> 最后更新：2026-07-26 00:34

# 220.5.3【Log】验收

> 用途：220.5 Enemy Runtime 壳、FSM、ActionLease、Timeline 泛型和 ScriptIntentEmitter 的原始运行时 Log 粘贴模板。
> 
> 请在 Unity PlayMode 中复制 Console 原文到对应区块。不要只填写“成功/失败”，也不要删除前后相邻的 instanceId、eventId、version、timestamp。

## 0. 阶段状态

```text
适配蓝图：220.5
220.5 代码蓝图落位：PASS
220.5 阶段性验收资格：PASS
220.5 阶段性实机验收：OPEN，等待本文件 Log 对账
```

本模板用于验收已经完成代码落位的 220.5，不代表当前已经通过 B1–B6 实机验收。

## 1. 测试元数据

```text
测试日期：
场景：
Enemy 名称：
Enemy instanceId：
Player 名称：
Player instanceId：
Enemy Loadout：
Enemy Entry：
Enemy Route：
Enemy Action：
Enemy ActionLease Version：
Unity Editor / PlayMode：
GameMainDebugSettings：CombatHit= / IntentArbitration= / InterruptFlow=
```

## 2. A0：Router、RuntimeReady、Locomotion

```text
在此粘贴原始 Log：
```

预期关键事实：

```text
[Feedback] channel=Router state=Ready subscriber=CombatFeedbackRouter
[Enemy] Runtime event=Ready result=Accepted entity=<enemy> instanceId=<enemy-id>
[Enemy] FSM event=Enter state=Locomotion entity=<enemy> instanceId=<enemy-id>
```

判定：`PASS / OPEN`

## 3. A1：ScriptIntentEmitter → IntentBuffer

```text
在此粘贴从 producer blocked 或 intent enqueue 开始的原始 Log：
```

预期关键事实：

```text
[Intent] channel=Enqueue result=Accepted host=Enemy kind=<kind> timestamp=<t> expire=<t>
```

判定：`PASS / OPEN`

## 4. A2：Pipeline Evaluate → Commit → ActionLease Arm

```text
在此粘贴同一 Enemy instanceId 的原始 Log：
```

预期关键事实：

```text
[Enemy] IntentArb phase=Resolve kind=<kind> result=Resolved route=<route> ... instanceId=<enemy-id>
[Enemy] IntentArb phase=Commit kind=<kind> result=Consumed route=<route> ... instanceId=<enemy-id>
[Enemy] ActionLease event=Arm version=<version> kind=<kind> route=<route>
```

判定：`PASS / OPEN`

## 5. A3：FSM Action → Lease Consume → Timeline → Animation

```text
在此粘贴 Action Enter 到 Attack BEGIN/EnemyAnim PLAY 的原始 Log：
```

预期关键事实：

```text
[Enemy] FSM event=Enter state=Action ... instanceId=<enemy-id>
[Enemy] ActionLease event=Consume version=<same-version> kind=<kind> action=<action>
[Attack] BEGIN source=Enemy action=<action> kind=<kind> duration=<seconds>
[EnemyAnim] PLAY action=<action> clip=<clip> kind=<kind> ...
```

判定：`PASS / OPEN`

## 6. A4：Attack HIT → Damage → Impulse

```text
在此粘贴同一个 eventId 的完整命中链原始 Log：
```

预期关键事实：

```text
[Attack] HIT ... target=<player> ...
[Resolve] eventId=<event-id> interaction=Hit target=<player>
[Feedback] channel=Damage result=Applied eventId=<event-id> ... target=<player>
[Feedback] channel=Impulse result=Applied eventId=<event-id> ... target=<player>
```

如果出现 `IgnoredByProfile`、`NoReceiver` 或目标仍无位移，原始保留并在备注中说明，不得改写为 Applied。

判定：`PASS / OPEN`

## 7. A5：ActionLease Complete

```text
在此粘贴动作自然结束到回 Locomotion 的原始 Log：
```

预期关键事实：

```text
[Enemy] ActionLease event=Complete version=<same-version>
[Enemy] FSM event=Complete state=Action ... instanceId=<enemy-id>
[Enemy] FSM event=Enter state=Locomotion ... instanceId=<enemy-id>
```

判定：`PASS / OPEN`

## 8. B0：RuntimeReady 阻挡

> 单独复制一个 Enemy 测试对象，移除 PresentationPort 或清空 Loadout 后运行。不要覆盖正常测试 Log。

```text
在此粘贴原始 Log：
```

预期：

```text
[Enemy] Runtime event=Ready result=Blocked reason=<reason>
[Intent] producer=ScriptIntentEmitter result=Blocked reason=runtime-not-ready ...
```

不得出现该对象的 `Enqueue Accepted`、`Action Enter` 或 `Attack BEGIN`。

判定：`PASS / OPEN`

## 9. B1：StateGate / Intent 保留

```text
在此粘贴动作占用期间第二次 Intent 的原始 Log：
```

重点检查：

```text
[Enemy] IntentArb phase=StateGate kind=<kind> result=Blocked ...
```

结合 `timestamp`、`expire` 和后续 Consume 判断 Intent 是 Queued、Flush 还是 Discard。

判定：`PASS / OPEN`

## 10. B2：Dead Cancel

```text
在 ActionLease 已 Consume、尚未 Complete 时使 Enemy 死亡，粘贴原始 Log：
```

期望：

```text
[Enemy] ActionLease event=Cancel version=<same-version> reason=Dead
[Enemy] FSM event=Enter state=Dead ...
```

若实际为 `reason=StateExit`，请原样保留，并将本项判为 `OPEN`，因为它没有证明 Dead 专属取消门已成立。

判定：`PASS / OPEN`

## 11. 连续两次出招计数

| 次数 | ActionLease Version | Arm | Consume | Attack BEGIN | HIT | Complete | 结果 |
|---|---:|---|---|---|---|---|---|
| 1 |  |  |  |  |  |  | PASS / OPEN |
| 2 |  |  |  |  |  |  | PASS / OPEN |

## 12. 最终对账

```text
Enemy instanceId 是否始终一致：PASS / OPEN
Player instanceId 是否始终一致：PASS / OPEN
同一 eventId 是否完成 HIT → Damage → Impulse 对齐：PASS / OPEN
同一 Lease Version 是否只 Consume 一次：PASS / OPEN
同一 Lease Version 是否只 Complete 或 Cancel 一次：PASS / OPEN
正式 Enemy 是否未使用 Enemy_Training 代替：PASS / OPEN
```

## 13. 最终结论

```text
220.5 代码结构：PASS
B1：PASS / OPEN
B2：PASS / OPEN
B3：PASS / OPEN
B4：PASS / OPEN
B5：PASS / OPEN
B6：PASS / OPEN
ActionLease：PASS / OPEN
RuntimeReady 阻挡：PASS / OPEN
Dead Cancel：PASS / OPEN

最终结论：220.5 验收通过 / 继续修正后复验
验收人备注：
```
