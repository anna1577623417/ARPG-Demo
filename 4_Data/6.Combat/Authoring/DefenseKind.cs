/// <summary>
/// 216.3 M5 L1 — 防御片段种类。
/// <para>Guard：前向 GuardVolume；Parry：招架窗（L2 Resolver）；Invincible：无敌窗。</para>
/// </summary>
public enum DefenseKind : byte
{
    Guard = 0,
    Parry = 1,
    Invincible = 2,
}
